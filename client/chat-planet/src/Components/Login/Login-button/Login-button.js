import Button from 'react-bootstrap/Button';

const LoginButton = () => {
    return (
        <div>
           <Button variant="primary" className='register-button'>Log In</Button>{' '}
        </div>
    );
};

export default LoginButton;